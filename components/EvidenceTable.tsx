import type { EvidenceManifestItem } from "@/types/vouch";
export function EvidenceTable({ evidence }: { evidence: EvidenceManifestItem[] }) { if (!evidence.length) return <p className="rounded-2xl border border-line bg-white/[.03] p-4 text-slate-400">No evidence files listed.</p>; return <div className="overflow-hidden rounded-3xl border border-line"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-white/[.05] text-slate-300"><tr><th className="p-4">File</th><th className="p-4">Type</th><th className="p-4">Size</th><th className="p-4">Walrus blob</th><th className="p-4">SHA-256</th></tr></thead><tbody>{evidence.map((item) => <tr key={`${item.name}-${item.sha256}`} className="border-t border-line/70"><td className="p-4 font-medium">{item.name}</td><td className="p-4 text-slate-400">{item.mimeType || item.type}</td><td className="p-4 text-slate-400">{(item.size / 1024).toFixed(1)} KB</td><td className="max-w-[180px] truncate p-4 text-brand-blue">{item.walrusBlobId}</td><td className="max-w-[220px] truncate p-4 font-mono text-xs text-slate-400">{item.sha256}</td></tr>)}</tbody></table></div>; }
