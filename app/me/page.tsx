import { MyProofs } from "@/components/MyProofs";
export default function MePage() { return <main className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-4xl font-black">My Proofs</h1><p className="mt-3 max-w-3xl text-slate-400">For the MVP, this page filters locally cached proofs by connected wallet. Production owner indexing can be added with Sui events or an indexer.</p><div className="mt-8"><MyProofs /></div></main>; }
