import type { CreateVouchInput, EvidenceManifestItem, VouchManifest } from "@/types/vouch";
export function createManifest(input: CreateVouchInput, wallet: string, evidence: EvidenceManifestItem[]): VouchManifest { return { schema: "vouch.project.v1", project: { name: input.name, tagline: input.tagline, description: input.description, category: input.category }, builder: { wallet, displayName: input.displayName || "", links: { github: input.repoUrl, x: input.xUrl || "", linkedin: input.linkedinUrl || "" } }, evidence, links: { repo: input.repoUrl, demo: input.demoUrl || "", sui: input.suiUrl || "" }, createdAt: new Date().toISOString(), version: 1 }; }
